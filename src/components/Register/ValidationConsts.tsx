// ensures that a given string has no whitespaces
export const REG_NO_WHITESPACE = /^\S*$/;
// ensures that a given string contains at least one numeric character
export const REG_INC_NUMERIC = /(?=.*[0-9])/;
export const NICK_MAX_CHARS = 15;
export const PASS_MIN_CHARS = 6;
